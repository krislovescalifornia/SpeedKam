.venv/
__pycache__/
*.pyc

# Python packaging / build artifacts
*.egg-info/
build/
dist/
.pytest_cache/

# Local, untracked config overlays holding real secrets / per-deployment values.
# These are deep-merged over the tracked config.yaml at load time. Never commit.
config.local.yaml
*.local.yaml
# The real off-site host config (copied from speedkam_config.example.php).
deploy/webhost/speedkam_config.php
captures/
captures_test/
**/.sync_queue/
speedkam_data/
# Legacy homography calibration artifacts (ignored so a lingering copy from
# before the crossing-time engine never gets committed).
calibration.json
calibration.ref.jpg
# YOLO weights (downloaded on demand by ultralytics)
*.pt
tools/simple_speed.json
