# Core dependencies (work on Windows + Raspberry Pi OS)
opencv-python>=4.8      # On Raspberry Pi you may prefer opencv-python-headless
numpy>=1.24
PyYAML>=6.0
Flask>=3.0             # Web dashboard (serve.py); on the Pi: sudo apt install python3-flask
requests>=2.28         # Off-site backup uploads; on the Pi: sudo apt install python3-requests

# Optional, Raspberry Pi only (native camera). Install with:
#   sudo apt install -y python3-picamera2
# picamera2 is NOT pip-installable cleanly; use the apt package on the Pi.

# Optional: vehicle attribute recognition (recognition.enabled: true).
# Heavy (pulls in PyTorch); install only if you want YOLO type/make/model.
# Vehicle COLOUR estimation needs NONE of this -- it's pure OpenCV.
#   pip install ultralytics
# ultralytics>=8.0
