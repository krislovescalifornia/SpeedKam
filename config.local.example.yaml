# =============================================================================
# SpeedKam LOCAL overrides — template.
#
# Copy this to `config.local.yaml` (untracked; gitignored) and fill in your real
# values. It is deep-merged OVER config.yaml at load time, so anything here wins.
# Put secrets and per-deployment values here; keep config.yaml shareable.
#
#     cp config.local.example.yaml config.local.yaml
#     # then edit config.local.yaml
#
# Generate a strong secret with:
#     python -c "import secrets; print(secrets.token_urlsafe(32))"
# and set the SAME value as $SECRET in the off-site host's speedkam_config.php.
# =============================================================================
# Fleet note: point `url` at ONE shared receiver for the whole fleet. Each camera
# identifies itself with a stable node id (from its CPU serial) that it sends on
# every request, and the receiver files each node's data under nodes/<id>/ on its
# own -- so this exact same value can be baked into a golden image and cloned to
# any number of cameras with no per-node edit.
backup:
  url: "https://yourdomain.example/speedkam/speedkam_receiver.php"
  secret: "CHANGE-ME-to-a-long-random-string"

# Optional: password-protect the LAN dashboard (HTTP Basic Auth). Uncomment and
# set a password to require login for the dashboard, APIs, live stream, and media.
# web:
#   auth:
#     username: admin
#     password: "CHANGE-ME-dashboard-password"
